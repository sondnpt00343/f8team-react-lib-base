import { Grid, Row, Column } from './'

describe('Grid', () => {
  it('is truthy', () => {
    expect(Grid).toBeTruthy()
  })
})

describe('Row', () => {
  it('is truthy', () => {
    expect(Row).toBeTruthy()
  })
})

describe('Column', () => {
  it('is truthy', () => {
    expect(Column).toBeTruthy()
  })
})
